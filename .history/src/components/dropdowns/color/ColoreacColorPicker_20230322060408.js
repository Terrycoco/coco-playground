import ColorPicker from "coloreact";
import {useState, useEffect} from 'react'
const ColorPicker = (props) => {
    return (
        <ColorPicker
            opacity={true}
            color={props.color}
            onChange={}
    )
}